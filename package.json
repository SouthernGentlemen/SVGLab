{
  "name": "svg-lab",
  "version": "0.1.0",
  "private": true,
  "description": "Local, deliberately unsafe 2D character animation laboratory.",
  "type": "module",
  "scripts": {
    "check:rig": "node pipelines/guards/rig.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "npm run check:rig && npm run typecheck && npm test"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vitest": "^5.0.0"
  }
}
