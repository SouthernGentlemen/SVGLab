{
  "name": "svg-lab",
  "version": "0.1.0",
  "private": true,
  "description": "Local, deliberately unsafe 2D character animation laboratory.",
  "type": "module",
  "scripts": {
    "teardown": "node pipelines/dev/lifecycle.ts teardown",
    "reset": "node pipelines/dev/lifecycle.ts reset",
    "launch": "node pipelines/dev/lifecycle.ts launch",
    "dev": "node pipelines/dev/lifecycle.ts dev",
    "prebuild": "node pipelines/guards/local-only.ts",
    "assert-local-only": "node pipelines/guards/local-only.ts",
    "build": "vite build",
    "check:rig": "node pipelines/guards/rig.ts",
    "build:parts": "node pipelines/sprite/build.ts",
    "check:sprites": "node pipelines/sprite/build.ts --check",
    "check:sockets": "node pipelines/guards/sockets.ts",
    "build:cosmetics": "node pipelines/wardrobe/build.ts",
    "check:wardrobe": "node pipelines/wardrobe/build.ts --check && node pipelines/guards/wardrobe.ts",
    "build:motions": "node pipelines/motion/build.ts",
    "check:motions": "node pipelines/motion/build.ts --check",
    "render:clip": "node pipelines/render/clip.ts",
    "render:figure": "node pipelines/render/figure.ts",
    "export:motions": "node pipelines/exchange/export.ts",
    "import:motions": "node pipelines/exchange/import.ts",
    "blender": "node pipelines/exchange/export.ts --blend",
    "check:exchange": "node pipelines/guards/exchange.ts",
    "check:footprint": "node pipelines/guards/footprint.ts",
    "check:blender": "node pipelines/guards/blender.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "npm run check:rig && npm run check:sprites && npm run check:sockets && npm run check:motions && npm run check:exchange && npm run check:wardrobe && npm run build && npm run check:footprint && npm run typecheck && npm test && npm run assert-local-only && npm run check:blender"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260911.1",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vite": "7.0.0",
    "vitest": "^5.0.0",
    "wrangler": "4.131.1"
  },
  "allowScripts": {
    "esbuild@0.25.12": true,
    "esbuild@0.28.1": true,
    "fsevents@2.3.3": true,
    "workerd@1.20260911.1": true
  }
}
